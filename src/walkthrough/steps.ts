// walkthrough/steps.ts — manages step progression through the session.
// Knows where we are, where we came from, and what's next.
// Reads from and writes to session state.
// One step at a time. Never skips. Never assumes a step is done.

// placeholder — logic coming next
