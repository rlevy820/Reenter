// walkthrough/check.ts — runs terminal commands with user consent and captures output.
// Always shows the command and explains why before running.
// Returns the raw output and feeds it to the AI for interpretation.
// This is how Reenter checks the real state of the machine — no guessing.

// placeholder — logic coming next
