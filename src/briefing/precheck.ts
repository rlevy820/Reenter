// briefing/precheck.ts — checks the actual state of the environment before the briefing.
// Runs real commands with user consent to find out what's already set up.
// Feeds real findings into the briefing so the user gets an accurate picture.
// Never assumes — always checks. Never runs anything without asking first.

// placeholder — logic coming next
